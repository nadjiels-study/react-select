
## Todo

- Debounce search
- Add autoload "onRender" or "onOpen"
- Add boilerplate rhf controlled select

## Improvements

- Asynchronous default value loading for when you need to fetch the initial value from an API
- Initial loading doesn't hide initial options
